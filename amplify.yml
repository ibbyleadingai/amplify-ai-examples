version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd claude-ai
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: claude-ai/.next
    files:
      - '**/*'
  cache:
    paths:
      - claude-ai/node_modules/**/* 